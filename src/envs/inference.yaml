name: inference
channels:
  - conda-forge
  - nodefaults
  - ufs-community
dependencies:
  - flash-attn=2.8.*
  - numpy=2.2.6.*
  - python=3.12
  # PyPI packages:
  - pip
  - pip:
      - anemoi-inference==0.10.*
      - anemoi-models==0.13.*
      - eagle-tools
